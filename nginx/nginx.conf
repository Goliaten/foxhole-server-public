# HTTP server - redirects to HTTPS
server {
    listen 80;
    server_tokens off;

    access_log /logs/access_80.log;
    error_log /logs/error_80.log debug;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://core:8001;  # Use service name from docker-compose
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        # return 301 https://$host$request_uri;
    }
}

# HTTPS server
# server {
#     listen 443 ssl;
#     server_tokens off;

#     access_log /logs/access_443.log;
#     error_log /logs/error_443.log debug;

#     # SSL configuration - adjust paths based on your certificate setup
#     # ssl_certificate /etc/letsencrypt/live/test.com/fullchain.pem;
#     # ssl_certificate_key /etc/letsencrypt/live/test.com/privkey.pem;
    
#     # Modern SSL configuration (from Mozilla)
#     ssl_protocols TLSv1.2; # TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
    
#     # HSTS (optional - uncomment after confirming HTTPS works)
#     # add_header Strict-Transport-Security "max-age=63072000" always;

#     # Proxy headers to FastAPI
#     location / {
#         proxy_pass http://core:8001;  # Use service name from docker-compose
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header X-Forwarded-Host $host;
#         proxy_set_header X-Forwarded-Port $server_port;
#     }
# }